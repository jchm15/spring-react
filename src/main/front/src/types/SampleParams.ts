export interface SampleParams {
    name: string;
    age: number;
}

// export interface SampleParams {
//     name: string;
//     age?: number;        // 필드 명 뒤에 '?'를 추가하여 age는 선택적으로 처리 가능케 한다
// }

